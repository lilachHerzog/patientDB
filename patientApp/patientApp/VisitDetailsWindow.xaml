<Window x:Class="patientApp.VisitDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="פרטי ביקור" Height="250" Width="400"
        FontFamily="Segoe UI" FlowDirection="RightToLeft">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="VisitDateText" FontWeight="Bold" FontSize="16" Grid.Row="0" Margin="0,0,0,10"/>
        <TextBlock x:Name="DescriptionText" Grid.Row="1" Margin="0,0,0,10" TextWrapping="Wrap"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,10">
            <Button x:Name="OpenPdfButton" Content="פתח PDF" Width="100" Margin="0,0,10,0" Click="OpenPdfButton_Click"/>
            <Button x:Name="OpenDocButton" Content="פתח DOCX" Width="100" Click="OpenDocButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
